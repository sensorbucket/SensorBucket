apiVersion: v1
kind: Service
metadata:
  name: http-ingress
  namespace: default
spec:
  selector:
    app: http-ingress
  type: ClusterIP
  ports:
  - name: http-ingress
    protocol: TCP
    port: 80
    targetPort: 3000

