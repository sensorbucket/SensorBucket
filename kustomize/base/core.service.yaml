apiVersion: v1
kind: Service
metadata:
  name: core
  namespace: default
spec:
  selector:
    app: core
  type: ClusterIP
  ports:
  - name: core
    protocol: TCP
    port: 80
    targetPort: 3000

