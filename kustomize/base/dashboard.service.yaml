apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: default
spec:
  selector:
    app: dashboard
  type: ClusterIP
  ports:
  - name: dashboard
    protocol: TCP
    port: 80
    targetPort: 3000

