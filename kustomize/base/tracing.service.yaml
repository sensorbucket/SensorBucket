apiVersion: v1
kind: Service
metadata:
  name: tracing
  namespace: default
spec:
  selector:
    app: tracing
  type: ClusterIP
  ports:
  - name: tracing
    protocol: TCP
    port: 80
    targetPort: 3000

